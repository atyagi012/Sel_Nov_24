package nov24;

import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.OutputType;
import org.openqa.selenium.TakesScreenshot;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;

public class BaseClass extends DriverClass {

	// WebDriver driver;
	DriverClass myDriver;

	/*
	 * @BeforeMethod public void openBrowser() { //driver = new ChromeDriver();
	 * 
	 * 
	 * myDriver = new ChromeDriver(); myDriver.executeScript("");
	 * 
	 * myDriver.getScreenshotAs(OutputType.FILE);
	 * 
	 * //myDriver.sess
	 * 
	 * 
	 * 
	 * ((JavascriptExecutor)driver).executeScript("");
	 * ((TakesScreenshot)driver).getScreenshotAs(OutputType.FILE);
	 * 
	 * 
	 * 
	 * }
	 * 
	 * @AfterMethod public void quitBrowser() {
	 * 
	 * }
	 */
}
